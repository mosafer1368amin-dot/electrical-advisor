import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.electrical.advisor',
  appName: 'Electrical Advisor',
  webDir: 'dist'
};

export default config;
